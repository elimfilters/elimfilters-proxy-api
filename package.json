{
  "name": "elimfilters-proxy-api",
  "version": "1.0.0",
  "description": "API for ELIMFILTERS part search",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.5",
    "google-auth-library": "^9.4.1",
    "googleapis": "^118.0.0",
     },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
```

**2. Haz push a GitHub**

**3. Railway hará redeploy automático** (o manualmente en Railway → Deploy)

**4. Mientras se instala, configura las variables de email en Railway → Variables:**
```
NODEMAILER_HOST=smtp.gmail.com
NODEMAILER_PORT=587
NODEMAILER_SECURE=false
NODEMAILER_USER=tu_email@gmail.com
NODEMAILER_PASS=tu_app_password
NODEMAILER_FROM=noreply@elimfilters.com
ALERT_EMAIL=destinatario@example.com
ENABLE_FILE_LOGGING=true
LOG_LEVEL=info
```

(Si usas otro proveedor de email, reemplaza los valores)

**5. Luego de que Railway haga redeploy exitoso, verifica en Logs que veas:**
```
[SECURITY INIT] API Key configurada: ✓
[STARTUP] ✓ ELIMFILTERS Proxy API escuchando en puerto 3000
