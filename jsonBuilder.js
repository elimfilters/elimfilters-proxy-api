// jsonBuilder.js

// La función principal debe ser exportada
export function buildFilterResponse(processedData) {
    // ... (lógica para mapear las 27 columnas)
    // Devolver { results: [response] }
}
